{
  "name": "mtg-commander-picker",
  "version": "1.0.0",
  "description": "A Tinder-style MTG Commander picker with filters and deck suggestions",
  "main": "index.js",
  "scripts": {
    "dev": "node node_modules/.bin/vite",
    "build": "node node_modules/.bin/vite build",
    "preview": "node node_modules/.bin/vite preview",
    "deploy": "npm run build && node node_modules/.bin/gh-pages -d dist",
    "postinstall": "chmod +x node_modules/.bin/* || true"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.0",
    "gh-pages": "^6.1.0"
  },
  "type": "module"
}